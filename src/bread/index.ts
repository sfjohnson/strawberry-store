import { initStore, lockKeys, unlockKeys, getKey, setKey, getAllKeysIterator } from './sqlite-store' 
// import { lockKeys, unlockKeys, getKey, setKey, getAllKeysIterator } from './memory-store'
export { initStore, lockKeys, unlockKeys, getKey, setKey, getAllKeysIterator }
